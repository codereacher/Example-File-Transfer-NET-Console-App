# Example-File-Transfer-NET-Console-App
This example .NET console application uses Chilkat for SFTP file transfer.

Chilkat .NET Class Library (32-bit) package is not included but can be installed using NuGet.
